services:
  api:
    build:
      context: .
      dockerfile: backend/Dockerfile
    image: meridian-dms-api:latest
    env_file:
      - path: ./backend/.env
        required: false
    environment:
      NODE_ENV: production
      PORT: 3000
    ports:
      - "5000:3000"
    init: true
    restart: unless-stopped
